# HANDOFF — training (frontend, training.cosmart.com.ar)

Actualizado: 2026-09-11. Este archivo reemplaza cualquier handoff anterior que hayas recibido pegado en el chat (ej. `HANDOFFcontenidosrrss.md`, `Handoff — IA para Emprendedores`) — esos describían un flujo de trabajo viejo que ya no existe, ver más abajo. Léelo entero antes de tocar código en este repo. Ver también `HANDOFF.md` en `AquiVane/cosmart-workers` para todo lo del backend.

## Novedades 11/09 — ajustes de legibilidad en la grilla mobile de tienda.html + acordeón en el selector de variantes

Vaneh miró la grilla de 2 columnas ya en producción (mobile real, no mockup) y pidió 4 ajustes puntuales sobre `tienda.html`, todos dentro del mismo `@media(max-width:600px)` de siempre:

- **Tamaño de foto**: sin cambios, quedó bien.
- **`.card h3`**: de `13.5px` a `16.5px` — lo pidió "definitivamente más grande, como mínimo 2 puntos".
- **`.card p.desc`**: de `10.5px` a `11.5px` — "un puntito" más grande.
- **Selector de variantes de la card de IA para Emprendedores**: antes las dos opciones (E-book / Campus + E-book) mostraban el precio completo (tachado + precio + USD, 3 líneas) siempre — a 173px de ancho de columna quedaba "un chocazo" (su palabra). Primer intento: acordeón (fila colapsada solo radio+nombre, tocarla despliega el precio) — Vaneh lo probó y no le gustó ("queda demasiado ancho... se tiene que deslizar mucho"), pidió explícitamente **una sola fila fija por opción con nombre + precio nada más**. Quedó así: cada `.mini-variant` es una fila única sin wrap (`flex-wrap:nowrap`), el nombre trunca con ellipsis si no entra, y se ocultan el precio tachado y la línea de USD (quedan definidos en el JS pero con `display:none!important` en mobile — hace falta el `!important` porque esos spans traen `style="display:block"` inline desde `renderMiniVariantes()`). Solo se ve el precio principal. Sin JS nuevo, solo se agregó la clase `mv-usd` al span de USD (y ya existía `mv-pricewrap`) para poder apuntarles desde CSS.
- **Desktop no se tocó**: todo este CSS vive adentro del media query mobile; en escritorio el selector sigue mostrando ambas opciones expandidas (con tachado y USD) como siempre. Verificado con Playwright (screenshot desktop sin cambios, sin overflow horizontal en mobile, y probado que seleccionar una variante y agregarla al carrito agrega la correcta).
- Pendiente aparte, no tocado en esta vuelta (no lo pidió esta vez): el texto de urgencia de IA para Emprendedores en la card mobile ("🔥 Precio especial de bienve...") se corta con ellipsis por `white-space:nowrap` — ya existía antes de estos cambios, queda documentado por si en algún momento se quiere acortar el texto como se hizo en su momento con el banner de arriba.

## Novedades 09/09 (3) — ficha PASTOR de vuelta, pero esta vez SOLO en mobile

La primera vez (07/09) se subió el cuerpo PASTOR completo para desktop también y a Vaneh "le quedó como el orto" en escritorio — se deshizo esa vez (ver más abajo, entrada vieja del 07/09 ya no aplica, esto la reemplaza). Ahora se reaplicó el mismo contenido (recuperado del commit revertido, sin reescribir nada) pero **gateado a mobile únicamente**:

- `render()` ahora decide `renderCuerpoPastorIA()` vs `renderCuerpoGenerico()` con `slug === 'iaprincipiantes' && esMobile()` — `esMobile()` es `window.matchMedia('(max-width:760px)')`, mismo punto de quiebre que ya usaba `.mobile-cta-bar` en este archivo.
- **En desktop, iaprincipiantes vuelve a mostrar el cuerpo genérico de siempre** (el mismo que usan `contenidosrrss`/`productos-ganadores`) hasta que Vaneh apruebe un diseño de escritorio — lo está revisando aparte sobre un Artifact que se le sigue actualizando ahí, no en el sitio.
- Se agregó un listener de `matchMedia('(max-width:760px)').addEventListener('change', ...)` que vuelve a llamar `render()` si alguien cruza el punto de quiebre sin recargar (rotar un tablet, agrandar la ventana) — el cuerpo PASTOR y el genérico son DOM distintos, no una cuestión de que el CSS se reacomode solo.
- El hero (título, bajada, precio, galería, selector) es compartido por ambos cuerpos y no cambia según el breakpoint — la bajada nueva ("Dejá de corregir a la IA...") ya se ve en desktop también, eso no formaba parte de la queja de Vaneh (solo el cuerpo de abajo).
- Mismas 9 imágenes de la vez pasada, recuperadas del commit revertido (no se volvieron a bajar de Drive).
- Probado con Playwright: mobile muestra el cuerpo PASTOR completo (idéntico a la versión que Vaneh ya había dicho que estaba bien), desktop muestra el genérico de siempre, sin errores de consola, sin overflow horizontal.

## Novedades 09/09 (2) — sacar la brújula de fondo en desktop + foto también en desktop

- **Se sacó el motivo decorativo de "brújula" (compás + grilla de puntos) del hero de `tienda.html`** — a Vaneh no le gustó cómo quedaba ("muy fea"), va a mandar una pieza mejor más adelante. Estaba `display:none` en mobile siempre (solo se activaba a partir de 640px vía un `<style>` inline), así que sacarlo no cambia nada en mobile. Se dejó el texto "Tu brújula digital" (el eyebrow), solo se sacó el SVG.
- **La foto de IA para Emprendedores (la misma real que ya se sumó a mobile) ahora también se ve en desktop** — `.card-photo` deja de estar `display:none` por defecto: pasa a tener su propio estilo de escritorio (bleed a los bordes redondeados de la card, `aspect-ratio:4/3`), y el override mobile (dentro del media query, `aspect-ratio:3/2` más compacto) sigue funcionando igual que antes. Las otras 2 cards (sin foto real todavía) no se tocaron.

## Novedades 09/09 — `tienda.html`: grilla mobile a 2 columnas + foto real en la card de IA Emprendedores

Pedido de Vaneh, revisado antes con un mockup en un Artifact (no directo a producción) hasta que lo aprobó. **Todo esto vive dentro de `@media(max-width:600px)` — desktop queda pixel-igual a como estaba, a propósito, ella lo pidió "solo mobile".**

- **Grilla**: `.grid` pasa de `repeat(auto-fit,minmax(250px,1fr))` (day siempre 1 columna en un celular, 250px no entran 2 veces) a `1fr 1fr` fijo en mobile, con `align-items:start` (las cards no se estiran a la misma altura — la de IA Emprendedores es más alta por el selector de variantes, y no hace falta que las demás se estiren para "rellenar" ese alto).
- **Foto real solo en la card de IA Emprendedores** (`PRODUCTO_VARIANTES.imagenUrl`, la misma que ya usa la ficha del producto) — las otras 2 (`Estrategia de Contenido`, `Cómo Identificar Productos Ganadores`) NO tienen foto real todavía, así que no se les inventó ningún placeholder visible al público; simplemente no muestran imagen, igual que antes. `.card-photo{display:none}` por defecto (fuera del media query) así nunca aparece en desktop.
- Toda la card se recompuso para mobile (padding, tamaños de fuente, saltos de línea de a 2 líneas en título/descripción) para que entre en la mitad del ancho — son overrides puntuales dentro del mismo media query, no se tocó ningún valor base.
- **Bug real encontrado y arreglado en el camino**: el selector de variante (E-book / Campus + E-book, solo existe en la card de IA Emprendedores) tiene una fila radio+nombre+precio que en desktop entra sobrada, pero en una columna de ~173px el precio (`white-space:nowrap`, no puede partirse en dos líneas) empujaba el ancho de toda la card fuera de la grilla — causaba scroll horizontal en toda la página. Se resolvió con `flex-wrap:wrap` + `flex-basis:100%` en el precio, así baja a su propia línea en vez de forzar el ancho de la card. También se agregó `min-width:0` a `.card` (por defecto los ítems de grid no se achican por debajo del contenido mínimo de sus hijos).
- **Segundo bug encontrado**: la card "Cómo Identificar Productos Ganadores" (con badge "+vendido") no tiene foto — el pill de "+vendido" (reposicionado en mobile para flotar sobre la foto de las cards que sí tienen) se superponía con el badge del rubro en las cards sin foto. Se separó con `:has(.card-photo)` — con foto, el pill flota sobre la imagen; sin foto, vuelve al mismo criterio de escritorio (poke arriba del borde de la card).
- **El overflow horizontal del `.cart-drawer` en mobile (`position:fixed;right:-100%`) es previo a esta tarea** — se confirmó comparando contra la versión sin tocar (mismo `scrollWidth` de más). No se tocó, queda fuera de esta tarea si en algún momento se quiere revisar.
- Pendiente si Vaneh manda fotos de los otros 2 productos: agregarles `imagenUrl` en `todosLosProductos()`/donde corresponda y el `<div class="card-photo">` ya soporta cualquier producto, no solo iaprincipiantes (el `.card-photo{display:none}` de base y el resto del CSS mobile no son específicos de un producto).
- Probado con Playwright: sin scroll horizontal nuevo, selección de variante y "Agregar al carrito" funcionando en mobile, desktop confirmado sin ningún cambio visual.

## Novedades 07/09 (3) — email obligatorio en `carrito.html`

Vaneh pidió sacarle el "(opcional)" al email del carrito, específicamente para que se sepa de una si un código de descuento aplica o no (en vez de descubrirlo recién al pagar). Antes el campo era decorativo (solo para el email de "dejaste algo en el carrito"), nunca bloqueaba nada.

- Nueva función `actualizarGatePago()` (reemplaza a `toggleConsent()`, mismo mecanismo `.pay-block-disabled` que ya existía para el checkbox de "esto es contenido digital"): ahora el bloque de pago (tabs + Mercado Pago + PayPal) queda gris/deshabilitado hasta que el email tiene formato válido **Y** el checkbox está tildado — antes solo dependía del checkbox. El texto de ayuda (`#payBlockHint`) cambia según cuál de los dos falta.
- El email se revisa en vivo mientras se escribe (`oninput`), no recién al salir del campo — la sensación es la de un campo obligatorio de verdad, aunque técnicamente no hay un `<form>` nativo acá (el pago se dispara por JS, no por submit).
- Efecto colateral bueno: como ahora hace falta el email ANTES de que aparezcan los métodos de pago, `resolverCodigoPendiente()`/`ctGuardarEmail()` (ver entrada de arriba, "cupones sitewide") ya tienen el email real mucho antes de intentar pagar — un código de "primera compra" como `EMBARQUE10` se valida y, si corresponde, se cae con el mensaje de error ANTES de que la persona llegue a ver el Brick, no después.
- Sigue sin violar la regla dura de "nunca sacar a la persona del checkout": no redirige a ningún lado, solo no muestra los métodos de pago hasta que completa el email — mismo patrón que ya existía para el checkbox de consentimiento.
- Probado con Playwright: sin email bloqueado (hint "Completá tu email..."), email inválido sigue bloqueado, email válido habilita los tabs de pago, y si se destilda el checkbox con el email ya puesto vuelve a bloquear con el hint del checkbox — sin errores de consola.

## Novedades 07/09 (2) — banner de bienvenida 10% OFF (`EMBARQUE10`) en `tienda.html`

Vaneh mandó una spec de implementación (`INSTRUCCIONES_IMPLEMENTACION.md`) más las 2 piezas ya diseñadas (Drive) para un banner de bienvenida: 10% OFF en la primera compra de cualquier curso/ebook de la tienda, código `EMBARQUE10`.

- **Imágenes**: `images/tienda/COSMART-Training-Banner-10OFF-Desktop-1920x400.webp` y `-Mobile-1080x640.webp`, bajadas de Drive tal cual las mandó (ya traen el texto/diseño integrado, no son piezas para recortar).
- **`tienda.html`**: el banner ocupa el espacio que antes era el placeholder "Espacio reservado para banner" (sección `.mid-banner-wrap` del medio, la que decía "imagen a confirmar" — NO la cinta roja fina de arriba con "🔥 Nuevo...", que es otro elemento y sigue igual). Es fijo, un solo `<picture>` con `source` para mobile (`max-width:768px`) — **no reusa el mecanismo de `BANNER_SLIDES`/carrusel** que ya existía ahí, porque ese hace `object-fit:cover` con un aspect-ratio fijo pensado para fotos recortables, y esta pieza ya trae texto integrado (recortarla habría cortado el "Código: EMBARQUE10"). El carrusel queda sin usar pero intacto en el JS por si en algún momento se necesita para piezas fotográficas rotables — `renderBannerCarousel()` ya no se llama, así que hay que volver a invocarla a mano si se reactiva.
- Toda la imagen es un `<a href="#programas">` clickeable: al hacer clic, `activarCupon('EMBARQUE10')` deja el código en `localStorage.ct_codigo` (mismo mecanismo que ya lee `carrito.html` al cargar — `leerCodigoPendiente()`/`resolverCodigoPendiente()`, el mismo que usa un link `?codigo=...`) y el navegador hace scroll a los programas (`id="programas"` nuevo en `.grid-wrap`, con `scroll-margin-top` para que no quede tapado por el header sticky).
- **Backend (`cosmart-workers`, `cosmart-training-core/src/index.js`, pusheado directo a `main`)**: el sistema de códigos de descuento existente (`CODES` en KV) estaba pensado para UN curso puntual por código (`courseId: 'contenidosrrss'`, etc.) — `EMBARQUE10` necesitaba aplicar a CUALQUIER producto del carrito, así que `courseId: 'ALL'` es ahora un valor especial que `carrito.html` interpreta como "descontá todos los ítems" (`codigoAplicaA()` en vez de comparar `courseId === slug`). El código se auto-siembra/autorepara solo (`asegurarCodigoEmbarque10()`, se llama cada vez que alguien valida "EMBARQUE10" — mismo patrón que ya existía para `IAPRINCIPIANTES50`), no hizo falta crearlo a mano desde el admin. Se puede desactivar desde el panel admin de códigos como cualquier otro (queda listado con `courseId: "ALL"` en la tabla).
- **"Solo para la primera compra"**: se agregó `primeraCompraSolo: true` al código. `GET /api/codigo` ahora acepta un `email` opcional — si el código lo requiere y hay email, chequea contra `USERS` si esa cuenta ya tiene algún curso comprado (`getUserCourseIds().length > 0`) y devuelve `elegible:false` si ya compró algo. **Limitación real, documentada para que no sorprenda**: como el email en `carrito.html` es opcional y recién se pide para el recupero de carrito abandonado, si alguien paga sin completarlo nunca se llega a chequear esto — el código se aplica igual. Esto es consistente con cómo ya funciona el resto del sitio (el monto que ve Mercado Pago/PayPal lo arma el navegador — `MP_WORKER`/`mp-productos-ganadores` nunca valida el precio contra un catálogo del lado del servidor, ver `PAYPAL_MIN_USD` como único piso real que existe hoy). Si en algún momento se quiere cerrar esa puerta de verdad, hay que validar el monto final contra el catálogo en el worker de pago antes de crear la preferencia — no se tocó nada de eso en esta tarea, es trabajo aparte y más sensible (toca el flujo de cobro).
- `carrito.html`: si el email ya está guardado (volvió a la página, o lo tenía de una compra anterior) se manda en la validación inicial del código. Si lo completa recién en el carrito (`ctGuardarEmail()`) y hay un código de "primera compra" ya aplicado, se re-valida solo (recarga la página, mismo patrón que aplicar/quitar código) — si resulta que ya compró algo, el código se cae mostrando el motivo, sin que se note un descuento fantasma en el total antes de pagar.
- Todo probado con Playwright: 10% aplicado a 2 ítems distintos a la vez (antes solo un código por curso), un código de un solo curso (`IAPRINCIPIANTES50`) sigue funcionando igual que antes (no se rompió nada), el rechazo por "ya compraste antes" cae solo al completar el email y muestra el mensaje, y el carrito sin ningún código sigue mostrando los precios normales. También: banner mobile carga la pieza mobile (no la de escritorio), clic deja el código guardado y hace scroll a los programas, sin errores de consola.

## Novedades 07/09 (1) — feedback sobre performance + los 5 cambios de imagen de `iaprincipiantes.html`, y reorden del banner de `tienda.html`

Después del commit de performance (WebP + `loading="lazy"` + dimensiones explícitas) y del de los 5 reemplazos de imagen puntuales, Vaneh mandó 5 capturas con feedback. Se investigó cada una por separado antes de tocar nada (regla dura: no asumir) y se confirmaron 3 bugs reales, se descartó uno como no reproducible en un primer paso pero terminó siendo real por otra causa, y se aplicaron 3 pedidos de ajuste. Todo en `iaprincipiantes.html` salvo el último punto.

- **Bug real — imagen de "Cómo vas a cursar" (columna curso) pisaba el título**: el reemplazo de imagen (`02-IA-Emprendedores-Vista-Campus.webp`) se había hecho con un `style="width:100%;height:auto;object-fit:contain"` inline que ignoraba el alto fijo de `.formato-img` (280px) y hacía que la imagen se desbordara hacia arriba, tapando el heading de al lado en desktop. Fix: se sacó el override inline y se dejó que la imagen use el comportamiento por defecto de `.formato-img img` (igual que la otra imagen de esa misma grilla, `portada-libro.webp`, que nunca tuvo el problema). Confirmado con Playwright: ambas imágenes de la grilla ahora miden exactamente lo mismo (top/bottom idénticos).
- **Bug real — imagen de FAQ estirada verticalmente**: al agregar los atributos `width`/`height` en el commit de performance, `.faq-card img` quedó sin ninguna regla de `height:auto`/`object-fit` (a diferencia de TODAS las demás clases de imagen tocadas ese día, que sí la tenían) — el navegador mantenía el alto crudo del atributo mientras el ancho se achicaba por `max-width:100%`, deformando la imagen en mobile. Esta imagen (`faq-imagen.webp`) no era parte de los 5 reemplazos pedidos — el daño fue un efecto colateral no buscado del cambio de performance, tal cual lo señaló Vaneh. Fix: se agregó `width:100%;height:auto` a `.faq-card img`. Confirmado con Playwright que ahora respeta el aspect ratio original (900×546).
- **Bug real, pero distinto al que se buscaba en un primer momento — "la sección de las estaciones está muy a la derecha, no hay margen" en mobile**: en un primer paso no se pudo reproducir mirando la sección de "Estación 02/03" tal cual aparecía en la captura. Investigando más a fondo se encontró la causa real: `.timeline` (todo el bloque de "Estación 00" a "Estación final") es la ÚNICA sección de contenido de texto de toda la página que no está envuelta en `.wrap` (que le da `padding:0 28px` a cada sección) — en mobile el texto queda literalmente pegado borde a borde de la pantalla, sin margen, a diferencia de cualquier otra sección. Fix: se agregó `padding:0 28px` a `.timeline` (con `box-sizing:border-box` global no afecta el ancho máximo en desktop, solo agrega aire cuando el viewport es angosto). Confirmado con Playwright: en mobile ahora hay ~28px de margen a cada lado; en desktop el bloque sigue midiendo exactamente 820px, sin cambios.
- **Pedido — imagen del certificado un poco más grande**: `.img-certificado-especial` pasó de `height:240px` a `height:256px` (clase propia, no se tocó `.tier-img img` que también usa la tarjeta del ebook). Se revisó visualmente que no se superpone con el texto de la tarjeta.
- **Pedido — más aire entre imágenes y texto en mobile** (además de lo ya ampliado en una vuelta anterior): se aumentaron varios gaps/márgenes solo en `@media(max-width:900px)`/`680px`: `.hero-inner` gap 40→50px, `.split` gap 56→64px, `.wide-img` margin 40/36→48/44px, `.sec04-extra-img` margin-top 44→52px, `.formatos-grid` gap 48→56px, `.faq-grid .faq-img` margin-bottom 8→28px. Nada cambió en desktop. Probado que no aparece scroll horizontal en ningún punto de la página (390px de viewport).
- **Pedido — sacar el `loading="lazy"` de todas las imágenes de la landing**: Vaneh prefiere que carguen todas de entrada (son livianas en WebP, no vale la pena diferir la carga a costa de la sensación de velocidad). Se sacaron los 12 `loading="lazy" decoding="async"` del archivo. Se mantuvo todo lo demás del trabajo de performance: conversión a WebP, atributos `width`/`height`, `fetchpriority="high"` + `preload` en la imagen del hero, y los `preconnect` de fonts/cosmart.com.ar.
- **`tienda.html` — el banner de "IA para Emprendedores ya está disponible" aparecía DEBAJO del header (logo+carrito)**: Vaneh lo quería arriba de todo, con el logo/carrito debajo — mismo patrón que ya se usa en `iaprincipiantes.html`/`404.html` (banner primero, header después). Se reordenó el HTML (el `<header class="tienda-header">` ahora va después de `<section class="top-banner">`), sin tocar CSS ni JS — el header sigue con su `position:sticky` de siempre. Confirmado en desktop y mobile con Playwright.
- **Pendiente, sin resolver todavía — "también le falta el menú acá, en desk y en mobile"**: no se tocó nada de esto. `tienda.html` nunca tuvo un menú de navegación (solo logo + botón de carrito, igual que el resto del sitio salvo `index.html`, que tiene un nav mínimo de logo + un solo link "Acceder →"). No quedó claro a qué se refiere exactamente ("menú" podría ser links a otras páginas del sitio, un menú de categorías de producto, o algo visual que le falta al banner) — preguntarle a Vaneh qué tiene que tener ese menú antes de inventar una estructura de navegación nueva, regla dura del proyecto de no asumir cambios no pedidos.
- Todo probado con Playwright contra el servidor local que simula GitHub Pages, en desktop (1280px) y mobile (390px): sin errores de consola propios del sitio, sin overflow horizontal, capturas de cada sección tocada comparadas antes/después.

## Novedades 06/09 (5) — miniembudo de la guía gratuita en /descargas/ + secuencia de email real

Vaneh corrigió la ubicación: el lead magnet de IA (y todos los que vengan después) viven bajo `/descargas/`, no sueltos en la raíz. Trajo además una spec muy detallada (referencia visual: `/iaprincipiantes`) para un miniembudo de 2 páginas, y pidió armar la secuencia de email real (antes solo existía un borrador propuesto en el chat).

- **`descargas/guiagratuita-iaprincipiantes.html`** (landing de descarga, indexable): header mínimo sin nav, hero de 2 columnas en desktop armado con 5 piezas independientes (`grid-template-areas`) para poder reordenarlas en mobile exactamente como pidió (label → título → texto → portada → formulario) sin duplicar HTML. Formulario de nombre+email contra el mismo `POST /leadmagnet/captura` que ya existía (ver `cosmart-workers`), ahora mandando también `utm`/`origenPagina`. Portada: placeholder (dashed box, ícono de libro en líneas finas) porque el archivo real de la guía todavía no existe — igual criterio que el resto del sitio ante archivos no subidos, nunca se usa la portada del curso pago ni se inventa una. Sección "5 usos concretos" y sección de alivio del dolor con botón secundario que ancla al formulario sin recargar.
- **`descargas/gracias-guiagratuita-iaprincipiantes.html`** (página de gracias, `noindex,nofollow`): entrega primero (botón de descarga directo a `.../files/leadmagnets/iaprincipiantes.pdf`, target `_blank` para no perder la página), después la transición al curso con una tarjeta resumida (no la landing completa) y el bloque de precio especial de bienvenida de 7 días — título, fecha y contador en vivo REALES, nunca inventados: se piden a `GET /leadmagnet/oferta?token=...` (token que devolvió `/leadmagnet/captura` al enviarse el formulario) y con eso se siembra `localStorage.ct_promo_iaprincipiantes_inicio` (misma clave que usa `iaprincipiantes.html`/`404.html`/`tienda.html`) para que la cuenta regresiva sea EXACTAMENTE la misma en todos lados y nunca se reinicie. Precio regular tachado + precio especial, igual patrón que el resto del sitio.
- **`iaprincipiantes.html`**: único cambio, un `recuperarOfertaPorToken()` que lee `?t=` de la URL (el link "Conocer el curso con mi precio especial" de la página de gracias, y los botones de los emails, llevan ese token) y hace el mismo pedido a `/leadmagnet/oferta` para sembrar el arranque real del contador si esta persona llega desde la guía gratuita en un dispositivo nuevo. No se tocó nada más de la landing — ni contenido, ni estructura, ni precios, ni checkout.
- **Eventos de analítica nuevos** (mismo beacon `/track` de siempre, sin nombre/email): `lead_magnet_view`, `lead_magnet_submit`, `lead_magnet_download`, `thank_you_course_offer_view`, `course_offer_click`.
- **`leadmagnet-iaprincipiantes.html` (la landing vieja) NO se tocó ni se borró** — Vaneh no lo pidió. Sigue existiendo en paralelo; preguntarle si quiere retirarla ahora que existe la nueva bajo `/descargas/`.

### Backend: secuencia de email real Día 0/2/4/6/7 (`cosmart-workers`, rama `claude/leadmagnet-ia-secuencia`, sin mergear)

Se armó de verdad la secuencia que antes era solo un borrador propuesto en el chat — vive en `workers/cosmart-training-core/src/index.js`, **pusheada a una rama nueva, no directo a `main`** (worker sin tests automatizados que además maneja el flujo de compra real — se prefirió dejarla para que se revise antes del próximo `workflow_dispatch` de deploy).

- `LEAD_MAGNETS.iaprincipiantes` ahora tiene `ofertaBienvenida: true` y `delays: [2,2,2,1]` (Día 2/4/6/7 después del de entrega en Día 0).
- Cada lead capturado (`handleLeadMagnetCaptura`) ahora tiene `token` (`randomToken()`), `offerStartedAt`/`offerExpiresAt` (7 días exactos desde la captura) y `utm`/`origenPagina` opcionales. Índice secundario `leadtoken:{token}` en KV `LEADS` para poder resolver el lead SIN conocer el email — el link de recupero nunca lleva el email en texto plano, solo el token.
- Nuevo endpoint público `GET /leadmagnet/oferta?token=...` — devuelve `nombre`/`magnetId`/`offerStartedAt`/`offerExpiresAt`, nunca el email. Lo consumen `iaprincipiantes.html` y la página de gracias de arriba.
- Email de entrega (Día 0) ahora incluye, SOLO para magnets con `ofertaBienvenida` (hoy únicamente iaprincipiantes), la frase "Por haber descargado la guía, activaste un precio especial de bienvenida..." + botón "Aprovechá este precio especial por 7 días" con el link de recupero — nunca en la landing de venta, regla dura de siempre.
- Los 3 emails de seguimiento se reescribieron y se sumó un 4to, siguiendo el tema exacto que pidió Vaneh: Día 2 mostrar el problema (por qué la IA da resultados genéricos), Día 4 presentar la solución (documento maestro + diferencia ebook/curso), Día 6 resolver objeciones (formato/acceso/certificado), Día 7 cierre honesto (avisa que el precio especial termina ESE día, con la fecha real de esa persona).
- La transición a una secuencia "normal, sin urgencia" después del vencimiento **ya estaba cubierta sin código nuevo**: todo lead ya se suscribe al newsletter evergreen de COSMART (`suscribirNewsletter`, mecanismo separado, sin fecha límite) desde el momento de la captura — sigue recibiendo esas ediciones mensuales después de que se apague la secuencia de urgencia, sin que haya que armar nada aparte.
- No se tocó la secuencia de `contenidosrrss` (no tiene esta oferta de 7 días).
- **Pendiente de Vaneh**: revisar el diff de esa rama y avisar cuándo mergearla + disparar el deploy manual (`workflow_dispatch`) para que quede en producción — hasta entonces, `/leadmagnet/captura` sigue funcionando con el comportamiento VIEJO (sin token, secuencia vieja de 3 emails genéricos).
- **Bloqueante real, ya documentado antes en `cosmart-workers/HANDOFF.md`**: el PDF de la guía (`leadmagnets/iaprincipiantes.pdf`) todavía no está subido — hasta que Vaneh lo suba desde el panel admin, el botón "Descargar mi guía" (página de gracias) y el link del email de entrega dan 404. No es un bug nuevo de esta sesión.

## Novedades 06/09 (4) — remates sueltos sobre la tarea del precio especial de bienvenida

- **Botones de las 2 tarjetas de precio alineados en desktop** (`iaprincipiantes.html`, `#comenzar`): `.tier-body` pasó a ser flex-column con `flex:1`, y su último hijo (el bloque de precio) usa `margin-top:auto` — así el botón queda siempre pegado abajo de la tarjeta, sin importar cuánto contenido tenga cada una arriba. Antes el espacio sobrante quedaba DESPUÉS del botón de la tarjeta más corta, no antes.
- **Card de "IA para Emprendedores" en `tienda.html` ya muestra precio tachado**, igual que "Cómo Identificar Productos Ganadores": `aplicarPromoLanzamientoIA()` ahora también setea `precioTachado`/`precioUsdTachado` (al precio regular) cuando el precio especial está activo — antes solo tocaba `precio`/`precioUsd`, así que el tachado que ya estaba en el HTML de `renderMiniVariantes()` nunca tenía nada que mostrar.
- **`/tienda/iaprincipiantes` (`404.html`) ahora muestra el bloque completo de oferta, no solo el badge corto** — Vaneh pidió específicamente "tal cual le aparece el banner arriba con la oferta y descontando el timer" (refiriéndose al bloque de `iaprincipiantes.html`). Se agregó el mismo bloque (título, explicación, fecha/hora individual, contador en vivo días·horas·min·seg, aviso de qué pasa al vencer) justo antes de las tarjetas de variante, con sus propios `renderPromoBlockIA()`/`iaFormatoContador()`/`iaFormatoFechaHora()` (mismo patrón duplicado que ya existía, no un archivo JS compartido). Acotado a `slug === 'iaprincipiantes'` — no aparece en ninguna otra ficha que use esta misma plantilla. El badge corto de arriba (`#urgenciaBadge`) se dejó como estaba, conviven los dos.
- Los tres cambios probados con Playwright contra el servidor local que simula GitHub Pages: botones a la misma altura (bounding box idéntico), tachado visible en la card de la tienda, bloque completo visible y con cuenta regresiva corriendo en `/tienda/iaprincipiantes`, y confirmado que se oculta solo al simular el vencimiento (adelantando el timestamp 8 días) sin afectar otros productos.

### Pendiente sin resolver, a preguntarle a Vaneh directamente

- **"Landing con fondo azul" de la guía de IA**: Vaneh dijo estar corrigiendo una landing de descarga de la guía gratuita de IA con fondo azul, distinta de la que se le mencionó. Búsqueda exhaustiva en este repo (`git ls-files "*.html"`, todos los 30 archivos revisados): el único archivo de este tipo es `leadmagnet-iaprincipiantes.html`, y su fondo es `--gcl:#F8F8F6` (crema clara), no azul. No existe ningún otro archivo de lead magnet de IA en este repo. **No se pudo identificar la página que menciona** — puede vivir en otro repo, en una herramienta de diseño aparte, o no estar todavía pusheada. Preguntarle la URL o el nombre exacto del archivo antes de tocar nada relacionado, para no volver a confundir dos páginas distintas.
- ~~Secuencia de emails después de la guía gratuita más simple que lo que Vaneh propuso~~ — **reescrita 06/09 (5)**, ver esa sección más arriba: ya sigue la cadencia Día 0/2/4/6/7 con link de recuperación por token y fecha real de vencimiento en cada email. Pusheada a una rama en `cosmart-workers` (`claude/leadmagnet-ia-secuencia`), pendiente de que Vaneh la revise y dispare el deploy.

## Novedades 06/09 (3) — "precio de lanzamiento" pasó a ser "precio especial de bienvenida"

Vaneh mandó una spec detallada (con diagrama de flujo) reemplazando SOLO la restricción de "no modificar precios" de la tarea anterior — todo lo demás de esa tarea seguía vigente. Cambios sobre lo ya hecho en "(2)" de más abajo:

- **Terminología**: "🔥 Precio de lanzamiento" → "🔥 Precio especial de bienvenida" en `iaprincipiantes.html`, `404.html` y `tienda.html` — mismo mecanismo de siempre (`ct_promo_iaprincipiantes_inicio`), solo cambió el texto.
- **Bloque nuevo en `iaprincipiantes.html`**, dentro de `#comenzar` (entre el `<h2>` y las tarjetas, sin sección nueva): título "Tu precio especial de bienvenida está activo", texto explicativo, fecha/hora individual de vencimiento (`toLocaleDateString('es-AR', ...)`) y un contador real con segundos (`Xd · Xh · Xm · Xs`, texto plano, se refresca con `setInterval` cada 1000ms — antes era cada 60000ms). Se oculta solo (`display:none`) cuando termina el plazo.
- **Precios de las 2 tarjetas reestructurados**: cuando el plazo está activo, cada una muestra "Precio regular: [tachado]" + la etiqueta "Precio especial de bienvenida" + el precio vigente como principal (`renderBloquePrecio()`). Vencido el plazo, solo queda el precio regular como precio principal, sin tachado ni etiqueta. En la tarjeta roja ("Ebook", `.tier.reco`) la etiqueta usa blanco en vez del rojo de marca — con rojo sobre fondo rojo no se leía.
- El mecanismo de fondo (localStorage por navegador, no se reinicia al refrescar, el precio que se manda al carrito se recalcula en el momento del click así nunca se cobra un precio vencido) es el mismo que ya se había construido — ver la entrada "(2)" de abajo para el detalle técnico completo.
- **No se tocó** `leadmagnet-iaprincipiantes.html` ni ninguna secuencia de email — el punto 7 de la spec de Vaneh (botón "Aprovechá este precio especial por 7 días" en la página de agradecimiento del lead magnet / en los emails) es opcional y esos emails viven en `cosmart-workers`, fuera de este repo. Si Vaneh lo pide, el link de esa página a `iaprincipiantes.html` YA dispara/recupera la cuenta regresiva correctamente sin tocar código — solo haría falta cambiar el texto del botón.
- Confirmado que `iaprincipiantes.html` no menciona la descarga de la guía gratuita en ningún lado (no había que sacar nada) y que no se usa "cupos limitados" ni "últimos lugares" en ningún lado del sitio para este producto.

## Novedades 06/09 (2)

- **Precios nuevos de `iaprincipiantes` + precio de lanzamiento por 7 días, por persona** (pedido de Vaneh). Regular: ebook $149.990/USD 97, campus+ebook $299.990/USD 197. Lanzamiento (los primeros 7 días de cada persona): ebook $79.990/USD 54, campus+ebook $149.990/USD 97.
  - **El conteo es por navegador, no una fecha global**: arranca la primera vez que esa persona abre `iaprincipiantes.html`, `/tienda/iaprincipiantes` o `tienda.html` (lo que sea primero) y se guarda en `localStorage.ct_promo_iaprincipiantes_inicio` (timestamp). Pasados 7 días desde ESE momento, el precio de esa persona vuelve solo al regular — cada visitante tiene su propio reloj. Es texto plano (`Xd Xh Xm`) que se refresca cada 60s con `setInterval`, no un `<canvas>` ni nada pesado.
  - **Mismo mecanismo duplicado en 3 archivos** (este sitio no comparte JS entre páginas, ver el resto de este handoff): `iaprincipiantes.html` (la landing, precios + timer en `#comenzar`), `404.html` (la ficha `/tienda/iaprincipiantes`, el timer vive dentro del badge `urgencia` que ya existía) y `tienda.html` (la card de la grilla, mismo badge `urgencia` de `PRODUCTO_VARIANTES`). Si en algún momento cambian los precios o la duración, tocar los tres. La clave de localStorage y la duración (`IA_PROMO_KEY`, `IA_PROMO_DURACION_MS`) están literalmente duplicadas en los tres — a propósito, mismo patrón que `CORE_API`/`ctTrack` en todo el sitio.
  - **Se compone con la promoción sitewide** (Cyber Monday, `/api/promocion`): el precio de lanzamiento/regular es la base, y si hay una promo global activa le aplica su % por encima (no la pisa). Si algún día conviene simplificar esto, ver `precioVigente()` en `iaprincipiantes.html` y `aplicarPromoLanzamientoIA()` en los otros dos.
  - **`productos-ganadores.html` NO tenía ningún timer** pese a que Vaneh lo mencionó como referencia ("como el que tiene productos-ganadores") — se revisó a fondo y esa página solo tiene un badge estático de "% OFF", sin cuenta regresiva real. El timer de arriba es nuevo, construido para esta tarea; si en algún momento se quiere el mismo mecanismo en `productos-ganadores.html`, es trabajo aparte.
  - Probado localmente con Playwright: precio de lanzamiento + timer corriendo en las 3 páginas, expiración simulada (adelantando el timestamp 8 días) confirmando que las 3 vuelven solas al precio regular y el timer/badge desaparece, y que `carrito.html` recibe el precio correcto en cada caso.

- **4 cambios de texto puntuales en `iaprincipiantes.html`**, pedidos con una lista cerrada de instrucciones (no tocar diseño/estructura/precios fuera de esos 4 puntos): el eyebrow del hero, el contenido de "Cómo vas a cursar" (la tarjeta de campus ahora aclara que incluye el ebook), el título + bullet + tag nuevo en la tarjeta de precio del campus ("EXPERIENCIA COMPLETA · OPCIÓN RECOMENDADA"), y la respuesta de la FAQ de certificado (aclara que es solo para la versión curso online). Ninguno tocó imágenes, links, botones ni la lógica de compra.

## Novedades 06/09

- **La tienda navy ("v2") pasó a ser la tienda principal, la arena/blanco quedó retirada.** Decisión final de Vaneh después de tenerlas corriendo en paralelo: "quiero que la versión azul quede como la principal finalmente". Cambios:
  - `tienda.html` ahora tiene el contenido que antes tenía `tienda-v2.html` (mismo re-skin navy/rojo "Cutral-Có"), con dos correcciones sobre ese contenido: se le agregó el beacon de analítica (`ctSessionId`/`ctTrack`, `page_view` + `add_to_cart`) que `tienda-v2.html` nunca había tenido, y `abrirProducto()`/`ct_origen_tienda` ahora apuntan a `/tienda/{slug}` y `'tienda.html'` en vez de a los `-v2`. **Si en algún momento se vuelve a tocar esta página, no perder ese beacon** — no se nota a simple vista porque no cambia nada visual, pero es lo que alimenta `/admin/metricas`.
  - `tienda-v2.html` quedó como un simple redirect a `/tienda.html` (mismo patrón que `producto.html`) — no se borró, para no romper links/bookmarks viejos.
  - `404.html` (el router de `/tienda/{slug}` y `/tienda-v2/{slug}`) ya no elige entre dos estéticas: la clase `body.v2` (navy) se aplica siempre, sin importar el prefijo de la URL. Los dos prefijos siguen funcionando y ambos vuelven a `/tienda.html` — no hace falta migrar links existentes a `/tienda-v2/...`.
  - Nada de esto tocó `PRODUCT_DB` ni el resto de la lógica de `404.html` (promociones, código de descuento, reseñas) — es un cambio puramente de "qué estética es la default", no de estructura.
  - Probado localmente de punta a punta (servidor HTTP con fallback a `404.html` simulando GitHub Pages + Playwright): `/tienda.html`, `/tienda/{slug}`, `/tienda-v2/{slug}` y el flujo completo de compra hasta `carrito.html`.

- **`iaprincipiantes.html` reescrito de punta a punta y ya publicado en `main`** — reemplaza el template genérico que tenía (grilla de galería en construcción, temario de 46 lecciones) por la landing final "IA para Emprendedores" que Vaneh definió en su Guía Maestra de Implementación (2 versiones del docx, la segunda "Sin fondos" es la que ganó). Copy, paleta (`--bg:#060F22`, rojo `#E02020`/`#FF3535`, nada de rosa/violeta/emoji), estructura de 17 secciones y mapeo de imágenes, todo aprobado por Vaneh en una sesión larga iterando sobre un preview en Artifact antes de tocar el repo real — no volver a rediscutir el diseño, solo iterar sobre pedidos puntuales nuevos.
  - **Imágenes**: viven en `images/iaprincipiantes-landing/` (nuevo directorio, 10 PNG + 1 JPG). Estaban embebidas como base64 en el HTML de trabajo (10MB+) — se extrajeron a archivos reales y se comprimieron con `pngquant --quality=70-90` (5.6MB → 1.8MB total, mismo resultado visual, illustration-style compresiona muy bien). Si se agregan imágenes nuevas a esta landing, pasarlas por pngquant antes de commitear — no subir PNG sin comprimir.
  - **Se mantuvo toda la integración existente del sitio** que el template viejo ya tenía y que no era parte del rediseño: beacon `POST /track` (`page_view` al cargar, `add_to_cart` en `elegirModalidad()`), promoción global (`GET /api/promocion`, aplicada en vivo sobre `MODALIDADES` y re-renderizada vía `renderPrecios()` — los precios en el HTML son solo el estado inicial antes de esa llamada), y el mismo carrito (`localStorage.ct_carrito`, forma `{slug:'iaprincipiantes', nombre, precio, precioUsd}`) que usan `carrito.html`/`tienda.html`/`404.html`. Si se vuelve a tocar esta página, no quitar ninguna de las tres cosas de arriba — no son parte del diseño visual, son plumbing del sitio.
  - **`leadmagnet-iaprincipiantes.html` NO se tocó** — es una página distinta (el lead magnet standalone, ver regla dura de landings de producto más abajo), Vaneh pidió explícitamente no confundirla ni borrarla al reemplazar `iaprincipiantes.html`.
  - Probado localmente de punta a punta antes de pushear (servidor HTTP local + Playwright): carga sin overflow en desktop/mobile, los 3 botones de compra escriben el ítem correcto en `ct_carrito` y redirigen a `carrito.html`, que lo muestra y calcula el total bien.
  - Se pusheó directo a `main` (con permiso explícito de Vaneh para este caso puntual) en vez de solo a la rama de la sesión — el sitio es estático sin build, así que eso ya lo dejó publicado.

## Novedades 03/09

- **Analítica de e-commerce completa, construida de punta a punta** (pedido explícito de Vaneh, "Arrancá todas las métricas del eccomerce incluso"). Detalle técnico del backend (D1, schema, `/track`, `/admin/metricas`, carrito abandonado) en `cosmart-workers/HANDOFF.md`. Del lado del frontend:
  - Beacon `POST /track` (anónimo, público, nunca bloquea la navegación — siempre 204 aunque el body esté mal formado) agregado a `tienda.html`, `404.html` (fichas `/tienda/{slug}`), `carrito.html`, `iaprincipiantes.html`, `contenidosrrss.html` y `productos-ganadores.html`: `page_view` al cargar, `add_to_cart` en cada "Agregar al carrito". `session_id` propio en `localStorage` (`ct_session_id`), separado de `ct_carrito`.
  - `carrito.html` suma un campo de **email opcional** (nunca bloquea ni gatea el pago, cumple la regla dura de abajo) — al completarlo dispara `checkout_start` con el monto total y el primer ítem del carrito. Es lo único que le da un destino al email de recupero de carrito abandonado.
  - Nueva pestaña **"Métricas"** en `admin/dashboard.html`: sesiones, page views, tasa de conversión, duración promedio de sesión, compras (ARS/USD), funnel completo (page_view → add_to_cart → checkout_start → purchase) con % de paso a paso, top productos agregados al carrito, carritos abandonados — todo contra `GET /admin/metricas?dias=N`, selector de rango (7/30/90 días).
  - **Todavía sin datos reales** — recién se publicó, va a empezar a llenarse con el tráfico real del sitio a partir de ahora. No confundir "la pestaña está vacía" con que algo esté roto.
- **Botón "Enviar notificación de prueba"** en la PWA de ventas (`admin/app/index.html`) — responde al pedido de Vaneh de poder confirmar que una notificación push real le llega sin tener que esperar a una venta ("Pwa no sé cómo verificar si llega la notificación real. Dice activada"). Pega a `POST /admin/push-test`, que reusa la misma suscripción guardada y devuelve el motivo exacto si falla (sin VAPID key, sin suscripción, suscripción vencida, etc.) en vez de un genérico "no funcionó".

## Novedades 02/09

- **Vaneh como figura de autoridad + reseñas reales**: el home (`index.html`) ya tenía una sección "Instructora" con foto/bio real de Vaneh Fernández (sacada de `brujula.html` en el repo `cosmart`, no inventada) — se le agregó al lado una sección de **Reseñas** que trae testimonios reales agregados de los 3 cursos (`GET /api/curso/:slug/testimonios-publicos`, mismo endpoint que ya usaban las landings). `contenidosrrss.html` no tenía sección de reseñas (a diferencia de `iaprincipiantes.html`/`productos-ganadores.html`, que sí) — se le agregó, mismo patrón, nunca inventa testimonios (si no hay ninguno, muestra el aviso de "recién se lanzó").
- **Fotos de portada para los 3 cursos reales**: Vaneh reportó que sus cursos ya cargados no aparecían en el panel admin para subirles foto — es porque `contenidosrrss`/`iaprincipiantes`/`productos-ganadores` están hardcodeados en `PRODUCT_DB` (404.html) y en `tienda.html`, no en el catálogo dinámico de "Productos creados" (ese es solo para productos simples creados desde el panel). Se agregó una sección nueva "Fotos de cursos" en `admin/dashboard.html` que sube a una key estable en R2 (`foto-curso-{slug}/foto`, ver `keyEstableDeCarpeta()` en el worker); `404.html` prueba esa URL siempre para los 3 cursos y si todavía no hay nada subido (R2 da 404), cae en el mismo placeholder de "imagen a subir" de toda la vida — no rompe nada mientras Vaneh no suba las fotos.
- **2 landings dedicadas de lead magnet**: `leadmagnet-iaprincipiantes.html` y `leadmagnet-contenidosrrss.html`, con el mismo copy que tenían los formularios embebidos que se sacaron el 29/08 (ver regla dura más abajo) — ahora viven en su propia página, con un upsell suave al curso completo abajo. Investigado a fondo el reclamo de Vaneh de que "se agregó un lead magnet a la página de contenidos" — no se encontró ningún formulario ahí (ni en `contenidosrrss.html`, ni en su `index.html`, ni en `cursos.html`), confirmado contra el `main` real. **Sigue sin resolver de qué página hablaba** — pedirle una captura si vuelve a aparecer.
- **PWA "COSMART Ventas"** (`/admin/app/`): notificación push a Vaneh cada vez que hay una venta real (Mercado Pago o PayPal, cualquier curso), con una tarjeta individual (`venta.html`) pensada para capturar y publicar (logo COSMART+Training, producto, monto, fecha). Primera versión usó OneSignal; Vaneh pidió explícitamente que no dependiera de una cuenta de terceros ("por qué tengo que descargar algo de un tercero"), así que se reemplazó por Web Push directo con `@pushforge/builder` (librería de cero dependencias hecha para Cloudflare Workers, solo usa Web Crypto API) + un service worker propio (`sw.js`) — VAPID generado localmente, sin que Vaneh tenga que crear cuenta en ningún lado. Ver detalle técnico completo en `cosmart-workers/HANDOFF.md`.
  - **Bug real de iOS encontrado y arreglado**: el botón "Activar notificaciones" no hacía nada. Dos causas, no una: (1) Safari en iOS solo expone `PushManager` cuando el sitio corre instalado desde la pantalla de inicio (modo standalone) — abierto como pestaña normal, no hay error, simplemente no existe la API; ahora se detecta con `matchMedia('(display-mode: standalone)')`/`navigator.standalone` y se avisa explícito. (2) `Notification.requestPermission()` tenía un `await` de otra operación asincrónica ANTES en el mismo handler del click — iOS exige que sea la primera operación async, sin nada previo, o lo ignora en silencio. Se reordenó. **Lección para cualquier feature de push/permisos en iOS**: pedir el permiso siempre primero, sin ningún `await` antes en el mismo handler, y no asumir que "no pasa nada" es un bug de lógica — puede ser un requisito de plataforma silencioso.
- **Reset de contraseña**: arreglados los 2 links rotos (uno le faltaba `.html`, el otro apuntaba al dominio viejo `cosmart.com.ar`) — ver detalle en `cosmart-workers/HANDOFF.md`.
- **Modal de reseña "tarda bastante en cargar"**: causa real encontrada — `handleProgressComplete` releía el contenido COMPLETO del curso (300KB+) en cada paso, no solo al terminar. Arreglado (solo relee en el último paso). **Sin confirmar por Vaneh todavía** que se sienta más rápido.
- **PDFs de lead magnet: ya subidos por Vaneh** (confirmado por ella el 02/09) — el flujo de entrega debería funcionar de punta a punta ahora. Pendiente #6 de la lista de abajo, cerrado.
- **Imágenes de Módulos 2-5 de `iaprincipiantes`: Vaneh sigue reportando que no las ve, sin resolver.** Se investigó a fondo del lado del código (ver conversación) — los archivos están en `main`, el seed con `<picture>` se cargó al KV, el worker deployado coincide con el código fuente, las rutas coinciden exacto. No se encontró nada roto de este lado. Sigue abierto — la próxima sesión que retome esto debería pedirle a Vaneh una captura de pantalla del paso exacto (ej. "Módulo 2, primer paso") donde no aparecen, en vez de seguir revisando a ciegas.

## ⚠️ Favicon de training: volvió a ser el gorrito, NO la C de COSMART (30/08)

El commit `9b698c0` ("Rediseñar favicon: C de COSMART...") del 29/08 a la noche había reemplazado el gorrito de graduación por la C de COSMART sobre fondo BLANCO + una franja roja con "TRAINING" abajo, describiéndolo como "pedido explícito de Vaneh". El 30/08 Vaneh se quejó de exactamente ese resultado en dos frases separadas: que el favicon de COSMART tenía fondo blanco (tiene que ser transparente, como en cosmart.com.ar) y que `training.cosmart.com.ar` seguía sin mostrar "el gorrito de training". O sea: **ese rediseño del 29/08 quedó revertido** — se regeneraron los 8 tamaños PNG + el `.ico` (4 tamaños embebidos: 16/32/48/64) desde la fuente correcta `images/favicon-training.svg` (birrete rojo, fondo transparente) con `cairosvg` + Pillow, y se le subió el cache-bust de las referencias a `?v=3` en los 30 HTML (favicons se cachean muy agresivo en el navegador, no alcanza con cambiar el archivo). **Si en algún momento alguien pide de nuevo "la C de COSMART con TRAINING abajo" para este sitio, confirmar con Vaneh antes de aplicarlo de nuevo** — ya se hizo una vez y se deshizo al otro día.

## Código de descuento en el carrito, propagado desde `?codigo=` en la URL (02/09)

`carrito.html` no tenía ningún campo para ingresar un código de descuento (Vaneh lo notó cuando el mail de premio por reseña, GRACIAS50, no tenía dónde aplicarse). Ahora:
- `404.html` (la página de un solo producto en `/tienda/{slug}` o `/tienda-v2/{slug}`) lee `?codigo=` de la URL al cargar y lo guarda en `localStorage.ct_codigo` -- así llega solo aunque la persona use "Comprar ahora" o "Agregar al carrito".
- `carrito.html` resuelve ese código pendiente (URL o `localStorage`) contra `GET /api/codigo` (mismo endpoint de `cosmart-training-core` que ya usaba `contenidosrrss.html`), y si el `courseId` del código coincide con algún ítem del carrito, le aplica el descuento SOLO a ese ítem (no al total entero) y recalcula `totalARS`/`totalUSD`, que son los mismos valores que ya usan el Brick de Mercado Pago y el botón de PayPal -- no hizo falta tocar `mp-productos-ganadores` porque esos endpoints ya confían en el `amount` que les manda el front (mismo modelo que `contenidosrrss.html` usa hace rato). También hay un input para tipearlo a mano. Aplicar/quitar un código dispara un `location.reload()` en vez de un segundo `render()`, seteando en `localStorage` en vez de mutar el DOM en caliente -- necesario porque un segundo `render()` después de que el Brick ya montó rompe el carrito (ver el comentario grande ya existente arriba de `initBrick()`).
- El email de premio por reseña (`emailPremioResena` en `cosmart-training-core`) ahora linkea a `tienda-v2/contenidosrrss?codigo=GRACIAS50` en vez de a la tienda general.

## ⚠️ Regla dura de `carrito.html` (pedido explícito de Vaneh, "marcarlo a fuego")

**BAJO NINGÚN PUNTO DE VISTA se saca a la persona del checkout para pagar en otra pestaña/navegador/sitio hosteado.** El pago tiene que resolverse siempre embebido en `carrito.html`. Por eso el 29/08 se sacó el link a `mpInitPoint` (el checkout hosteado de Mercado Pago) que aparecía como "alternativa" cuando el Brick tardaba — eso violaba la regla. Si el Brick o PayPal fallan, la única salida válida es un botón de "Reintentar" inline (ver `reintentarBrick()`) o pedirle que escriba un mail — nunca un link de pago externo.

## ⚠️ Regla dura de landings de producto individuales (pedido explícito de Vaneh, 29/08)

**NUNCA un lead magnet (ni ningún otro desvío) en `iaprincipiantes.html`, `contenidosrrss.html`, `productos-ganadores.html` o cualquier landing standalone equivalente.** Pasó una vez el 28/08 (se les agregó un formulario de guía gratis) y Vaneh lo marcó como error explícito el 29/08: en la página de venta de UN producto, TODO tiene que llevar a la compra, sin distracciones — ni siquiera una oferta "más chica" gratis. Ver también CLAUDE.md. El mecanismo de lead magnet en sí (KV `LEADS`, `/leadmagnet/captura`, secuencias) sigue existiendo y es válido — el problema nunca fue el mecanismo, fue dónde se mostraba.

`iaprincipiantes.html` en particular es y va a seguir siendo una landing 100% dedicada, al estilo `cosmart.com.ar/productos-ganadores` — Vaneh la va a usar para campañas propias. No agregarle nada que no sea directamente parte de la venta de ese curso puntual.

## Novedades 30/08

- **Lead magnet sacado de `iaprincipiantes.html` y `contenidosrrss.html`** — ver regla dura arriba. Las dos páginas volvieron a ser 100% conversión, sin el formulario de guía gratis que se les había agregado el 28/08.
- **`tienda-v2` ahora tiene sus propias URLs de producto**: `/tienda-v2/{slug}`, igual que `/tienda/{slug}` para la tienda original. `404.html` (el router de URLs limpias) ahora detecta el prefijo real de la URL (`tienda` vs `tienda-v2`) y aplica la estética navy o la arena/blanco sobre la misma `PRODUCT_DB` — no hay dos bases de datos de producto, es la misma ficha con distinto CSS (clase `body.v2`). `tienda-v2.html`'s `abrirProducto()` es la ÚNICA diferencia de JS a propósito entre las dos tiendas (apunta a `/tienda-v2/` en vez de `/tienda/`) — todo lo demás sigue siendo idéntico entre `tienda.html` y `tienda-v2.html`. También setea `localStorage.ct_origen_tienda` correcto al entrar directo a una ficha de `/tienda-v2/`, para que "Seguir comprando" en `carrito.html` vuelva a la tienda correcta aunque no hayas pasado por el grid primero.

## Novedades 29/08

- **Bug real arreglado en `carrito.html`**: ni el Brick de Mercado Pago ni el checkbox de "contenido digital" funcionaban bien. Causa raíz: el init de `toggleConsent()` y el `IntersectionObserver` que dispara el Brick se ejecutaban ANTES de que `render()` terminara de insertar el HTML del carrito (`render()` espera un fetch async al catálogo de upsell) — `getElementById` devolvía `null`, tiraba un error silencioso, y esa parte de la página se quedaba pegada en su estado default (el hint "marcá la casilla" visible para siempre, el Brick sin trigger). Se arregló consolidando todo el init post-render en el mismo `.then(render)`. PayPal en sí no tenía este bug (se inicializa al click del tab, no al cargar), pero compartía el mismo síntoma visual porque el checkbox bloqueado tapaba todo.
- **"Seguir comprando" ahora respeta si viniste de `tienda.html` o `tienda-v2.html`**: ambas tiendas escriben `localStorage.ct_origen_tienda` al cargar; `carrito.html` lo lee para el link de "Seguir comprando" y el de "Ver los programas disponibles" del carrito vacío (antes ambos apuntaban siempre a `tienda.html`, aunque hubieras entrado por la v2).
- Sacado el link a Mercado Pago hosteado del fallback cuando el Brick tarda — ver regla dura arriba.

## Novedades 28/08

- **⚠️ Todo lo de esta sesión estuvo días sin publicarse en `main`** (quedó en la rama de trabajo desde el commit `51b7c1d` en adelante, 13 commits) — GitHub Pages sirve `main`, así que nada de esto era visible en el sitio real hasta que Vaneh lo notó al pedir ver `tienda-v2.html` y se publicó recién el 28/08 a la noche (fast-forward limpio, sin conflictos). Si en algún momento algo "no aparece" en el sitio aunque el código esté commiteado, lo primero a chequear es si esa rama llegó a `main` — no asumir que sí solo porque está pusheada a origin.
- **Escala nueva en contenidosrrss: "Prospección B2B en LinkedIn"** (`escala-linkedin`, 2 pasos + mini-evaluación de 5 preguntas). Se portó el sistema de mini-evaluación (que antes solo tenía `iaprincipiantes`) a `cursos/contenidosrrss/acceso.html` — ver funciones `esUltimoDeEscala`/`evaluacionPendiente`/`renderEvalBox`/`enviarEvaluacion` y el CSS `.eval-box` al final del `<style>`.
- **Sistema de lead magnets completo**: formularios de captura en `iaprincipiantes.html` y `contenidosrrss.html` (sección antes del footer, función `enviarLeadMagnet()`), backend en `cosmart-workers` (KV `LEADS`, `POST /leadmagnet/captura`, secuencia de 3 emails de seguimiento que se corta sola si la persona compra). **Pendiente de Vaneh**: subir los dos PDFs finales (contenido ya escrito y entregado, ella hace el diseño) — ver sección de pendientes abajo para el nombre exacto de archivo que tienen que llevar.
- **`iaprincipiantes` ahora tiene mini-evaluaciones reales** (35 preguntas, 5 por módulo/sección) — antes el mecanismo de quiz existía pero nunca se había cargado un banco de preguntas, así que Vaneh solo veía cajas de texto sin ningún test bloqueante. Seedeado y deployado.
- **4 infografías de resumen por módulo** (Módulo 2 a 5) agregadas a `iaprincipiantes` — subidas a `cursos/iaprincipiantes/images/` en este repo, referenciadas vía `<picture>` responsive en el contenido del curso (mismo patrón que ya usaba `contenidosrrss`).
- **Newsletter evergreen consolidada entre TODAS las verticales de COSMART** (pedido explícito de Vaneh: "TODOS... si entra como creadora, como marca, todos"), en dos pasadas: primero Design/Euforia/Hub/Brújula/Método, después compra de cualquier curso (incluido Productos Ganadores). Dedupe (nunca 2 veces en el mismo mes a la misma persona) garantizado por diseño, no fue necesario ningún cambio extra. Ver detalle completo en `cosmart-workers/HANDOFF.md`, sección "Newsletter evergreen".
- **`tienda-v2.html`**: segunda versión de la tienda, estética navy/rojo real de la capacitación de Cutral-Có, para comparar contra `tienda.html` (que no se tocó). Mismo HTML/JS, solo CSS distinto — carrito, checkout y promociones funcionan igual en las dos.
- **Doc de copy de emails**: Vaneh nunca podía ver el texto completo de los emails automáticos, solo los asuntos — se armó un Google Doc en su Drive con las 5 secuencias completas (34 emails: newsletter, 2 lead magnets, Cutral-Có, contenidosrrss).

## ⚠️ Lo primero: el flujo de trabajo cambió

Los handoffs viejos asumían una sesión LOCAL sin acceso a GitHub, editando archivos sueltos que Vaneh subía a mano. Ahora esta sesión (y cualquiera que herede este repo) tiene acceso directo de lectura/escritura vía git (commit + push a `main`, o a la rama que te hayan indicado), y el sitio es 100% estático servido por GitHub Pages — no hay build ni deploy manual, el push a `main` ya lo publica.

**Varias sesiones de Claude corren en paralelo sobre este repo.** Es normal que `git push` rebote — resolver con `git fetch origin main && git merge origin/main --no-edit -m "..."` y volver a pushear. Nunca `--force`, nunca reescribir historia ajena.

## Arquitectura (ya resuelta, no volver a discutir)

- Sitio 100% estático (HTML/CSS/JS sin build step), servido por GitHub Pages. `CNAME` apunta a `training.cosmart.com.ar`.
- **URLs limpias `/tienda/{slug}`** se logran con el truco de GitHub Pages: `/404.html` en la raíz se sirve para cualquier ruta no encontrada, sin cambiar la URL del navegador. `404.html` lee el slug de la URL, busca el producto en `PRODUCT_DB` (o lo trae del catálogo dinámico) y renderiza la ficha completa ahí mismo. **Todos los links/assets dentro de ese archivo tienen que ser absolutos (`/...`)**, porque relativos resolverían contra la URL profunda, no contra la ubicación real del archivo.
- `carrito.html` usa `localStorage` (`ct_carrito`) como fuente de verdad del carrito, compartido entre `tienda.html`, `carrito.html`, `404.html`, `iaprincipiantes.html`, `productos-ganadores.html`.
- El backend vive en `AquiVane/cosmart-workers` (worker `cosmart-training-core`, ver su propio `HANDOFF.md`). `CORE_API` en cada página apunta a ese worker.
- **Páginas de venta individuales que SON y deben seguir siendo standalone** (no redirects): `iaprincipiantes.html`, `contenidosrrss.html`, `productos-ganadores.html`. Sirven como landings de marca para campañas propias, aparte de sus entradas en `/tienda/{slug}`. `producto.html` sí es un shim de redirect (`?slug=` viejo → `/tienda/{slug}`), ese no se toca.
- **La página vieja `cosmart.com.ar/productos-ganadores`** (repo `AquiVane/cosmart`, solo lectura) sigue viva a propósito — el contenido ya está migrado acá pero la vieja no se borra, pueden circular links a cualquiera de las dos.

## Catálogo vigente (ver tabla completa en `cosmart-workers/HANDOFF.md`)

`iaprincipiantes` cambió de nombre visible a **"IA para Emprendedores"** el 27/08 (slug/URL/`<title>`/meta-description siguen diciendo "principiantes" a propósito, por SEO — solo cambió lo que ve la gente: h1, cards, botones, emails). Aparece hardcodeado con ese nombre en: `tienda.html`, `iaprincipiantes.html`, `404.html` (`PRODUCT_DB` y `UPSELL_BASE`), `carrito.html`, `productos-ganadores.html`, `admin/dashboard.html` (mapa de nombres de campaña), `cursos/iaprincipiantes/acceso.html`. Si se vuelve a renombrar, tocar los mismos 7 archivos + el worker.

## Sistema de promociones (agregado 27/08)

Igual lógica que en el backend (ver ese handoff). Del lado frontend, 4 páginas aplican la promoción global al cargar (`cargarPromocionGlobal`/`aplicarPromoSiCorresponde`, fetch a `GET /api/promocion`): `tienda.html`, `404.html`, `iaprincipiantes.html`, `productos-ganadores.html`. Todas mutan los objetos de producto en memoria (nunca el HTML fuente), así que desactivar la promo en el admin alcanza para que todo vuelva a precios normales sin tocar código.

## Banner de la tienda (carrusel)

`tienda.html` tiene un carrusel automático (`BANNER_SLIDES`, hoy vacío — Vaneh tiene que subir las imágenes reales) con autoplay + swipe + puntos. Tamaños definidos: **1600×500px desktop, 1080×1080px mobile** (vía `<picture>`+`<source media>`, no es la misma imagen escalada — son dos encuadres distintos a propósito). Las imágenes de galería de cada producto (no el banner) usan un contenedor de `aspect-ratio` fijo, así que ahí SÍ alcanza con una sola imagen por producto.

## Favicon

`images/favicon*` (ico + png en 8 tamaños) son compartidos por casi todas las páginas del sitio — cambiarlos una vez actualiza el ícono en todo el sitio sin tocar HTML por página.

**Diseño actual (29/08, reemplaza al birrete rojo viejo)**: la C real del logo de COSMART (recortada de `Logo completo COSMART.png` en la raíz del repo, colores originales navy `#004AAD`/rojo `#FD0808`, no los `--az1`/`--ro1` del sitio) sobre fondo blanco arriba, "TRAINING" en blanco sobre franja roja sólida (`#E02020`, el rojo del sitio) abajo. **Es size-adaptive a propósito**: 16/32/48px usan solo la C sola (sin texto, mismo fondo blanco) porque el texto se vuelve ilegible a ese tamaño sin importar la tipografía — probado en tamaño real con PIL/LANCZOS, no solo en el preview grande (la lección de la sección de errores de abajo seguía siendo válida). 64px en adelante (ícono ampliado, apple-touch-icon, preview de WhatsApp/redes) llevan el diseño completo con texto. Si hay que retocarlo de nuevo, mantener ese corte size-adaptive — no asumir que un solo diseño sirve para todos los tamaños.

## Pendiente real / decisiones abiertas

1. **Subir las imágenes reales del banner de la tienda** — el carrusel está armado, solo falta contenido (`BANNER_SLIDES` en `tienda.html`). Prompt sugerido ya entregado a Vaneh.
2. **Crear un código de descuento nuevo** ahora tiene UI en `admin/dashboard.html` (sección "Todos los códigos") — antes solo se podían ver, no crear.
3. Reducir el peso del ebook de `iaprincipiantes` (130MB) — lo hace Vaneh, no bloqueante para nada más.
4. "2x1" real (llevar 2, pagar 1) no está construido — requiere carrito con cantidades, feature aparte.
5. **Meta Pixel/Conversions API** (worker `mp-productos-ganadores`, ver `cosmart-workers/HANDOFF.md`): Vaneh confirmó el 27/08 que quiere activarlo para cuando corra anuncios — está limpiando su cuenta de Meta Ads antes de pasar el Pixel ID/token. Falta: (a) que ella cargue `META_PIXEL_ID` y `META_CAPI_ACCESS_TOKEN` como secrets vía el workflow "Set Worker Secret (admin)", y (b) agregar el script del Pixel del lado del cliente a las páginas de la tienda (hoy no existe, solo quedó el aviso server-side de Purchase) — ofrecido, sin confirmar todavía.
6. ~~Subir los 2 PDFs de los lead magnets~~ — **hecho, confirmado por Vaneh el 02/09.**
7. ~~Rediseño de la tienda estilo Cutral-Có~~ — **hecho 28/08**: `tienda-v2.html` (navy `#060f22` + rojo, cards con borde fino, motivos de compás/grilla de puntos, sacados de las páginas reales del PDF de Cutral-Có — no inventado). `tienda.html` no se tocó. Mismo JS exacto en las dos (verificado byte a byte), probado con Playwright (carrito, agregar, checkout). Falta la opinión de conversión que pidió Vaneh — dada en el chat, no repetida acá.
8. ~~Las 4 imágenes sin nombre de módulo claro~~ — **resuelto**: Vaneh confirmó que son las mismas de las infografías (repetidas), para la futura landing de venta de `/iaprincipiantes`, no para wirear en el contenido del curso.
9. **Imágenes de Módulos 2-5 de `iaprincipiantes` (sin resolver, ver "Novedades 02/09")** — Vaneh sigue sin verlas, código revisado a fondo sin encontrar nada roto. Pedirle una captura del paso exacto antes de seguir.
10. **Facturación ARCA**: código y endpoint de prueba (`POST /admin/facturar-test`) desplegados en Homologación, pero Vaneh todavía no generó el certificado/clave (`ARCA_CERT_PEM`/`ARCA_KEY_PEM`/`ARCA_CUIT`) ni los cargó como secrets — sin eso, no se puede probar ningún CAE real. Ver instrucciones completas ya dadas en el chat (comandos `openssl` + pasos en el portal de ARCA).
11. **Notificaciones push de la PWA de ventas**: ahora tiene un botón de prueba (ver "Novedades 03/09") — Vaneh puede tocar "Enviar notificación de prueba" ella misma en vez de esperar una venta real. Falta que confirme que le llega.
12. ~~Carrito abandonado + analítica tipo e-commerce~~ — **construido 03/09** (ver "Novedades 03/09" y `cosmart-workers/HANDOFF.md`): funnel, sesiones, duración promedio, email opcional en el carrito, escaneo diario de carritos abandonados con email de recupero. Sin datos reales todavía (recién publicado) — nada más pendiente de este lado, solo esperar tráfico real.
13. **Tiendas dedicadas por producto** (03/09) — Vaneh quiere que cada producto tenga su propia "tienda" en vez de ser solo una entrada en `tienda.html`/`404.html`. Se acordó estructurarlo por subpath/slug (ej. `/iaprincipiantes`), NO por subdominio propio — el conversion lift real viene de la página estar 100% enfocada en un solo producto, no de la URL. Se armó un primer draft visual de `iaprincipiantes` como artifact (fuera del repo, para iterar el diseño antes de tocar código real) -- nav propia con anclas, hero con CTA arriba de todo, sección de precios separada, mismo sistema de colores/tipografía del sitio. **Todavía no se decidió la estructura final ni los redireccionamientos** (qué pasa con `/tienda/iaprincipiantes` y `iaprincipiantes.html` actuales) -- no confundir el draft con algo ya construido en el repo.
14. **Traducción automática al detectar país de origen** (03/09) — pedido nuevo, sin alcance definido (¿todo el sitio o solo landings de venta? ¿traducción estática pre-generada o dinámica del lado del cliente?). Ver detalle en `cosmart-workers/HANDOFF.md`.
15. **Pagos internacionales (IVA europeo / Factura E / sales tax EE.UU.)** — decisión ya tomada en otra sesión (no usar Merchant of Record, codearlo), cero código todavía. Detalle completo en `cosmart-workers/HANDOFF.md`.

## Backlog general de Vaneh (Google Sheet, cargado 27/08)

Vaneh mantiene una planilla de Google Sheets con tareas pendientes de TODOS sus productos, no solo training.cosmart.com.ar. Se leyó completa el 27/08 y se repartió por repo. Los repos de cada producto (todos en la cuenta `AquiVane`, todos con push habilitado):

- **training.cosmart.com.ar** → este repo (`AquiVane/training`)
- **hub.cosmart.com.ar** (Marketing Hub, panel de gestión de clientes/contenidos/tareas) → repo `AquiVane/hub`. El backend es el worker `marketing-hub` en `cosmart-workers`.
- **Euforia** (creadoras/marcas, incluye el formulario "UNIRME") → repo `AquiVane/euforia`. Backend: worker `euforia-worker` en `cosmart-workers`.
- **cosmart.com.ar** (sitio institucional viejo + bitácora/blog) → repo `AquiVane/cosmart` (existe una nota vieja de "solo lectura" para no tocar `productos-ganadores` ahí, pero SÍ se puede escribir el resto del sitio, como la bitácora).
- Otros repos de la cuenta vistos pero sin tareas de la planilla todavía: `shows`, `talent`, `mpg`, `germanaquino`, `rumbovoraz`, `comunicos`, `desing`, `euforia-ugc`, `ClaudeIA`, `linkvault` — no tocar sin que Vaneh los mencione.

### Pendientes de training.cosmart.com.ar (este repo)

- ~~Migrar Productos Ganadores a training, sin sacar la vieja~~ — **la planilla dice "In Progress" pero YA ESTÁ HECHO** (ver arriba, `productos-ganadores.html`) — avisar a Vaneh para que actualice el estado en su sheet.
- ~~Actualizar `cursos.html`~~ — **HECHO 27/08**: el link de Productos Ganadores apuntaba a la página vieja `cosmart.com.ar/productos-ganadores.html`, se cambió a `training.cosmart.com.ar/productos-ganadores`. También faltaba `iaprincipiantes` en la grilla pública de "Recursos disponibles" (solo tenía Productos Ganadores + contenidosrrss) — se agregó, y se agregó `iaprincipiantes` al mapa `ICONOS` del dashboard privado (`🤖`).
- ~~"Hacer e-commerce como Spotify"~~ — **confirmado por Vaneh 27/08: ya está hecho, es `tienda.html`**. Cerrado, no requiere nada más.
- ~~"Arreglar curso de estrategia de contenidos que Claude arruinó"~~ (contenidosrrss) — **CONFIRMADO Y ARREGLADO 27/08**: Vaneh confirmó en mayúsculas que sí, el problema era el goteo — las 6 Escalas no se tenían que desbloquear juntas. Se restauró el goteo diario (`calcUnlocked()` en `cursos/contenidosrrss/acceso.html`, una Escala nueva por día desde la fecha de embarque, igual que `calcularDesbloqueadas()` del worker de Productos Ganadores), se corrigió el texto de la UI y de la bienvenida del curso (KV) que todavía decía "no hay goteo", y se agregó el mensaje de bloqueo correcto ("todavía no se desbloqueó" vs. "confirmá tu fecha primero"). Cerrado.
- ~~Infografías de contenidosrrss~~ — **HECHO 27/08**: Vaneh ya las tenía diseñadas y subidas al repo desde el 25/08 (`cursos/contenidosrrss/images/`, pares desktop+mobile: algoritmo-ig, formatos-validados, prisma-hamburguesería, claude-skill) pero nunca se habían wireado al contenido real — solo un placeholder genérico (`puerto1.png`) estaba en uso. Se reemplazó ese placeholder y se agregaron las otras 3 infografías (con `<picture>` responsive) a sus Escalas correspondientes en el seed JSON (`cosmart-workers/workers/cosmart-training-core/seed/contenido-contenidosrrss.json`), y se corrió el workflow "Seed Course Content (KV)" para publicarlas. Cerrado.
- LinkedIn B2B: **confirmado 27/08 por Vaneh — SÍ, ampliar contenidosrrss (no curso aparte), con enfoque de prospección, siempre desde estrategia de contenido** (no ventas/CRM). Investigación hecha vía WebSearch el 27/08, resumen para la próxima sesión que arme el copy:
  - *Contenido*: el algoritmo de LinkedIn en 2026 premia punto de vista por sobre volumen; perfiles personales rinden mucho más que páginas de empresa (la jugada ganadora es postear desde el perfil personal y amplificar desde la página); 3-5 posts/semana desde el perfil personal es lo ideal, más de 1 por día frena el alcance; mejor horario martes-jueves 8-10am y 12-2pm; para pymes/servicios el foco tiene que ser generar consultas concretas, no solo awareness (momentos personales + contenido de solución + CTA claro).
  - *Prospección*: connection requests personalizados (mencionar algo puntual del perfil/contenido/empresa) tienen 55% más aceptación que genéricos; InMail personalizado rinde ~20% mejor que el genérico y hay que ser breve (3 párrafos cortos ganan a 6 largos); cadencia sugerida: día 0 solicitud de conexión, día 2 mensaje de valor (no venta directa), día 7 check-in si no hubo respuesta.
  - Con esto ya se puede armar el módulo/Escala nueva de contenidosrrss — falta que Vaneh revise el enfoque de contenido, y ahí arrancamos con el copy.
- Lead magnet gratuito: **el pedido original de Vaneh quedó ambiguo hasta para ella misma** ("eso quizás quise decir" — 27/08). Dos lecturas posibles: (a) una guía de IA como lead magnet DEL curso `iaprincipiantes`, o (b) una guía de IA como lead magnet PARA atraer gente hacia `contenidosrrss` (el "para emprendimientos" del pedido original sugiere que podría ser esto último). Sin resolver — aclarar con Vaneh antes de escribir una sola línea de copy.
- Adaptar el contenido de Cutral-Có a un curso genérico "Marketing básico para comerciantes" — ya se había dado una opinión sobre esto en una sesión anterior (no lanzarlo aparte, usarlo como lead magnet o producto institucional B2B), retomar esa conversación con Vaneh antes de construir.

### Pendientes de hub.cosmart.com.ar (repo `AquiVane/hub`, sin explorar todavía en detalle)

Urgentes: forzar cambio de contraseña temporal en primer login, asignar tareas a colaboradores dentro del panel de cada cliente, que se mande el mail cuando arroban a alguien en un comentario de una tarjeta de contenido, ~~el botón de editar de un link abre "crear nuevo" en vez de editar~~ (**arreglado 27/08** — links viejos sin campo `id` hacían que `openLinkModal` no encontrara el link correcto; ahora se les asigna un id estable al cargar, ver `js/app.js`/`loadAllData`), asignar automáticamente la tarea de cargar el logo al crear un cliente, soportar que un cliente tenga más de una cuenta conectada (ej. Martín Cañeque con Pharus y MC, o COSMART con Talent/Design/Shows/Euforia) con panel individual + panel conjunto, ~~corregir el botón de eliminar del panel de control (posición/tamaño de la cruz)~~ (**arreglado 27/08** — el botón `.btn-danger` no tenía borde mientras los demás `.btn` sí, causando la diferencia de altura; se igualó la caja de todos los `.btn` con `border:1px solid transparent` + `justify-content:center`). **Nuevo bug reportado 27/08, sin arreglar todavía**: en `admin/index.html`, el menú de "⋮ Más opciones" de cada cliente (`client-row-menu`) se abre recortado/clippeado por la fila de abajo en vez de flotar por encima de todo el contenido — Vaneh ve "✏️ Editar" cortado y no llega a ver "🗑 Eliminar" (que si está en el código, condicionado a `user.role === 'admin'`). Es un problema de posicionamiento/z-index del menú, no de que falte el botón.
Alta: agregar el link directo al admin de training y al admin de Euforia dentro del hub, título separado "CONTENIDOS"/"TAREAS" en Inicio con conteos que alimenten el reporte por mail, favicon del panel, tareas con rango de fechas/sub-tareas tipo proyecto.
Media/sin priorizar: lista de clientes colapsable tipo menú, integración con Meta/Instagram Marketing API (botón "Importar desde Meta"), categorías + drag-to-reorder en Links, Ctrl+Z en campos, límites de duración de video por plataforma (Vaneh tiene que dar los datos), auto-guardado de borrador, pegar links/imágenes inline en Ideas, botón "Crear contenido desde esta idea", revisar una lista vieja de 19 issues del banco de contenidos, confirmar que Links/Sitio Web persisten bien, probar emails de Brevo con un cliente nuevo, panel de control de ventas para el cliente COSMART, sistema de notificaciones de tareas resueltas por colaboradores con filtro por cliente, panel personal de tareas para Vaneh, que las tareas del sitio web también aparezan en el listado general, subir más el azul de fondo del logo de COSMART, pegar imágenes en el resultado final de una pieza de contenido (con límite de peso), plan de suscripción barata (USD 3/mes o USD 30/año) para agencias/creadores/pymes que suban contenido, foto de cliente en PNG con fondo blanco en vez de bordes punteados.
Sin estado en la planilla: conectar aperturas de email de las plataformas internas de COSMART al hub, panel interno compilado de tareas de gestión + verticals con disparo de mails.

### Pendientes de Euforia (repo `AquiVane/euforia`, formulario `unirme.html`)

Alta: agregar edad al formulario de UNIRME, agregar localidad de residencia, agregar checkbox de "Acepto presencias físicas + contenido".

### Pendientes de cosmart.com.ar (repo `AquiVane/cosmart`)

- Artículo de blog nuevo en `/bitacora` una vez por mes — recurrente, tarea de contenido de Vaneh, no de código.
- Redireccionamiento a la bitácora — **la planilla dice "Hecho"**, no requiere nada.
- Subir el PDF de liderazgo y marketing — pendiente, falta el archivo de Vaneh.

## Reglas duras aprendidas a los golpes (no repetir errores)

- **Vaneh se comunica solo en español.** Nunca responder en inglés.
- **Nunca hacer cambios no pedidos.** Ejemplo real: se cambió `.grid` a flexbox en `tienda.html` sin que lo pidiera → corrección explícita ("Nunca te dije que hicieras ese cambio, como estaba estaba bien, volvelo"). Preguntar antes de "mejorar" algo que no se quejó.
- `iaprincipiantes.html` se convirtió por error en un simple redirect en una sesión — tuvo que revertirse porque es una landing de marca standalone a propósito. No volver a convertirla en redirect.
- `.price.discount` scopeado de más (`.price-row .price.discount`) no aplicaba dentro de la barra sticky mobile — si agregás una clase de color condicional, chequear que no esté sobre-scopeada a un contenedor específico.
- No re-renderizar el DOM después de que un SDK de pago (MP Brick, PayPal Buttons) ya se montó ahí — no son idempotentes.
- Al pedir imágenes/prompts, dar tamaños concretos en px y aclarar si mobile/desktop necesitan encuadres distintos (banner) o si un solo tamaño alcanza (galería) — no asumir que se entiende solo.
- **No asumir que un servicio de terceros es aceptable solo porque tiene capa gratis.** Se armó una primera versión de las notificaciones push con OneSignal (requiere que Vaneh cree una cuenta) y ella lo rechazó explícito ("por qué tengo que descargar algo de un tercero, en vez de todo ese bardo por qué no lo desarrollás vos"). Se reemplazó por una librería que corre 100% dentro de nuestro propio Worker (`@pushforge/builder`, Web Crypto, sin cuenta externa). Para cualquier feature nueva que implique "conectar con un servicio externo", conviene preguntar primero si el self-hosted es aceptable, en vez de asumir que la opción más rápida de implementar es la que quiere.
- **Push/permisos del navegador en iOS Safari tienen reglas propias, no genéricas**: `PushManager` no existe fuera del modo standalone (PWA instalada desde "Agregar a pantalla de inicio"), y `Notification.requestPermission()` tiene que ser la primera operación async del handler del click, sin ningún `await` previo — si no, se ignora en silencio sin ningún error. Si algo "no hace nada" en un flujo de permisos en iOS, sospechar de estas dos reglas antes que de otra cosa.

## Cómo seguir desde acá

Este archivo se actualiza **al menos una vez por semana**, o antes si hubo cambios grandes — ver `CLAUDE.md` en la raíz del repo para la instrucción exacta. Si lo encontrás desactualizado, actualizalo vos mismo antes de terminar la sesión.
